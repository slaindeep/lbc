export interface HeroContent {
  id: number;
  image: string;
  title: string;
  description: string;
  caption: string;
}

export interface HeroProps {
  className?: string;
}

// You can add more types here as needed
